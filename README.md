## Rocket Sweep
This repository contains code for https://rocketsweep.vercel.app
- `web/` contains the website UI frontend.

### Dev Setup
- TODO: doc `.env` (see `.env.example`)
- TODO: doc `cd web`, `npm install`, `npm start`, etc
- See `MAINTENANCE.md` for the worktree workflow, Vercel deployment settings, and CI-style validation commands

### Style
- TODO: doc `npm run pretty` etc.
